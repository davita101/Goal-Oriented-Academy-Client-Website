import { RefreshCcw } from 'lucide-react'
import React from 'react'

export default function Submit() {
  return (
    <>
      <p className='flex items-center justify-center gap-2'>Submitting <RefreshCcw className='animate-spin' /></p>
    </>
  )
}
